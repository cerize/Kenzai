<%= simple_form_for [@sprint, @task], remote: true do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.association :node, collection: @sprint.tasks, label: "Dependence", prompt: "Select a dependence, if any" %>
  <%= f.submit %>
<% end %>
